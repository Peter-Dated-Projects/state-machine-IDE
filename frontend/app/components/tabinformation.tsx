// supported languages
export const DEFAULT_CLASS_TEXT = `
class BaseState(StateComponent):
    def __init__(self, name: str, id: str):
        self._name = name
        self._id = id
        
    def update(self):
        pass
`;
